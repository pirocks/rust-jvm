use crate::ir_compiler_common::PointerValueToken;
use crate::ir_compiler_common::special::IRCompilerState;

impl IRCompilerState<'_> {
    pub fn emit_local_load_pointer(&mut self, local_var: u16) -> PointerValueToken{
        todo!()
    }
}
